// User is logged in
export const handleLogin = (setIsLoggedIn) => {
    setIsLoggedIn(true);
  };
  
// User is logged out
export const handleLogout = (setIsLoggedIn) => {
    setIsLoggedIn(false);
  };